#!/usr/bin/env zsh
# Info: the master of this file(alias) resides at ~/..dot-files/dot-zshrc

### PATHS
DOTFILES_PATH=~/..dotfiles
VOLTA_PATH=~/.volta
# HOMEBREW_PATH=~/.homebrew
# DOCKER_VMS_PATH=~/.docker-vms
# ICLOUD_DRIVE_PATH=~/Library/Mobile\ Documents/com\~apple\~CloudDocs
# PERSONAL_DRIVE_PATH="/Volumes/Personal-Drive"

export PATH="~/.volta/bin:$PATH"

alias reload='source $DOTFILES_PATH/dot-zshrc'
alias cl=clear

# Brew (https://docs.brew.sh/Installation#multiple-installations-unsupported)
# eval "$($HOMEBREW_PATH/bin/brew shellenv)"

# Load this last
eval "$(starship init zsh)"
