#!/usr/bin/env bash
# this will create backups of all dotfiles that might be replaced.
git config --global init.defaultBranch master
git config --global user.name "Dustin Gelelegonya"
git config --global user.email "github@gelegonya.com"
